[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "eval-fixture"
version = "0.1.0"
description = "DepSafe 评估样本"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.95.0",
    "python-multipart==0.0.31",
]
